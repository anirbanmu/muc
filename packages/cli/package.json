{
  "name": "@muc/cli",
  "version": "1.0.0",
  "description": "CLI for MUC project.",
  "type": "module",
  "main": "dist/cli.bundle.cjs",
  "scripts": {
    "build": "esbuild src/cli.ts --bundle --platform=node --target=node20 --outfile=dist/cli.bundle.cjs --format=cjs --banner:js='#!/usr/bin/env node'",
    "start": "node --env-file=../../.env dist/cli.bundle.cjs",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write . --ignore-path ../../.prettierignore",
    "format:check": "prettier --check . --ignore-path ../../.prettierignore"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@muc/common": "*"
  },
  "devDependencies": {}
}
